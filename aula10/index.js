//Dados Primitivos: String, number, undefined, null, boolean, symbol.
//Tipos de Datos: 1) Tipo primitivo; 2) Objeto (Arrays, Functions);
const nome = 'Fernando' //string
const nome1 = "Fernando" //string
const nome2 = `Fernando` //string
const num1 = 10; //number
const num2 = 10.52; // number
let nomeAluno; // undefines = não aponta a local nenhum na memória
let sobrenomeAluno = null; // Nulo -> não aponta pra nenhum local da memória
const aprovado = false; // Boolean = true ou false (lógico)